import { scopedType } from '../../utils';

export const SET_PLAYING = scopedType('playback/SET_PLAYING');
export const PLAY = scopedType('playback/PLAY');
export const PAUSE = scopedType('playback/PAUSE');
